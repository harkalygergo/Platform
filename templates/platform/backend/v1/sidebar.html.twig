<div class="sidebar col-md-3 col-lg-2 p-0 bg-dark vh-100">
    <div class="offcanvas-md offcanvas-end" tabindex="-1" id="sidebarMenu" aria-labelledby="sidebarMenuLabel">
        <div class="offcanvas-header">
            <h5 class="offcanvas-title" id="sidebarMenuLabel">PLATFORM</h5>
            <button type="button" class="btn-close text-white" data-bs-dismiss="offcanvas" data-bs-target="
            #sidebarMenu" aria-label="Close"></button>
        </div>
        <div class="offcanvas-body d-md-flex flex-column p-0 pt-lg-3 overflow-y-auto">

            {% for sidebarMenuKey, sidebarMenuValue in sidebarMenu %}
                <h6 id="{{ sidebarMenuKey }}" class="sidebar-heading d-flex px-3 mb-1 text-white text-uppercase fw-bold">
                    <!-- if the sidebarMenuValue['icon'] is defined and not empty, then render the icon -->
                    {% if sidebarMenuValue['icon'] is defined and sidebarMenuValue['icon'] is not empty %}
                        <i class="{{ sidebarMenuValue['icon'] }}"></i>
                    {% endif %}
                    {{ sidebarMenuValue['title'] }}
                </h6>
                {% if sidebarMenuValue['children'] is defined and sidebarMenuValue['children'] is not empty %}
                    <ul class="nav flex-column mb-auto">
                        {% for sidebarMenuChildKey, sidebarMenuChildValue in sidebarMenuValue['children'] %}
                            <li class="nav-item">
                                <a class="nav-link d-flex align-items-center gap-2 text-white-50" href="{{ path(sidebarMenuChildValue['route']) }}">
                                    <i class="{{ sidebarMenuChildValue['icon'] }}"></i>
                                    {{ sidebarMenuChildValue['title'] }}
                                </a>
                            </li>
                        {% endfor %}
                    </ul>
                {% endif %}
                <hr>
            {% endfor %}
            <div class="text-center text-white">
                <small>
                    <i class="bi bi-stack"></i> PLATFORM &copy; 2024 brandcom.
                </small>
            </div>
        </div>
    </div>
</div>
